import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import { Avatar, IAvatarProps } from "./Avatar";

<Meta
  title="Avatar"
  component={Avatar}
  argTypes={{
    size: {
      type: { name: "string", required: false },
      defaultValue: "md",
      description: "Size of the avatar",
      table: {
        type: { summary: "string" },
        defaultValue: { summary: "md" },
      },
      options: ["sm", "md", "lg", "xl"],
      control: {
        type: "select",
      },
    },
    shape: {
      type: { name: "boolean", required: false },
      defaultValue: circle,
      description: "Loading State Toggle",
      table: {
        type: { summary: "boolean" },
        defaultValue: { summary: "circle" },
      },
      options: ["circle", "square"],
      control: {
        type: "boolean",
      },
    },
  }}
/>

export const Template = (args) => <Avatar {...args}>{args.children}</Avatar>;

# Avatar

This is the `RusticUI` Avatar Component. It supports all the HTML Avatar attributes and some extra.

<Canvas>
  <Story
    name="sm"
    args={{
      size: "sm",
      children: "sm",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="md"
    args={{
      size: "md",
      children: "md",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="lg"
    args={{
      size: "lg",
      children: "lg",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="xl"
    args={{
      size: "xl",
      children: "xl",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Accesskey Binding"
    args={{
      size: "md",
      accessKey: "m",
      children: "Shortcut Bound",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props List

<ArgsTable of={Avatar} />
